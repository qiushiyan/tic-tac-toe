import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { alignItems: "center", paddingTop: 120 },
  logo: {
    height: 150,
    maxWidth: "60%",
    resizeMode: "contain",
  },
  buttonContainer: {
    marginTop: 20,
  },
});

export default styles;
